type Product = {
    title: string,
    description: string,
    midia: string[],
    price: number,
    comparisonPrice: number,
    category: string,
    subcategory: string | null,
};

export default Product;